{
  "name": "encore",
  "private": true,
  "version": "1.0.0",
  "description": "Convenience scripts only — deliberately not npm workspaces (hoisting would complicate mongodb-memory-server and the two Docker build contexts for no real gain here).",
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "seed": "npm --prefix server run seed",
    "test": "npm --prefix server test && npm --prefix client test",
    "test:server": "npm --prefix server test",
    "test:client": "npm --prefix client test",
    "lint": "npm --prefix server run lint && npm --prefix client run lint",
    "lint:server": "npm --prefix server run lint",
    "lint:client": "npm --prefix client run lint",
    "typecheck": "npm --prefix client run typecheck",
    "build": "npm --prefix client run build",
    "test:e2e": "playwright test --config=QA/playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
